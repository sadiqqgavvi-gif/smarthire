# SmartHire

![CI](https://github.com/sadiqqgavvi-gif/smarthire/actions/workflows/ci.yml/badge.svg)

AI-powered interview preparation platform built with the MERN stack — practice technical, behavioral, and situational interviews, run mock interview sessions with voice input, get instant AI-generated feedback, and track your progress on a performance dashboard.

![SmartHire](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/03-hero.png)

## Table of Contents

- [Features](#features)
- [Tech Stack](#tech-stack)
- [Screenshots](#screenshots)
- [Project Structure](#project-structure)
- [Getting Started](#getting-started)
- [How Answer Evaluation Works](#how-answer-evaluation-works)
- [API Overview](#api-overview)
- [Testing](#testing)
- [Deployment](#deployment)
- [Roadmap](#roadmap)
- [About This Project](#about-this-project)
- [License](#license)

## Features

- **AI-evaluated answers** — every response gets a 1–10 score plus strengths, weaknesses, and a concrete improvement plan, generated by OpenAI, an optional Python evaluator, or a built-in heuristic fallback
- **Guardrailed scoring** — answers that are too short, off-topic, or gibberish are automatically capped, so feedback stays trustworthy regardless of which evaluator produced it
- **Practice & Mock Interview modes** — Technical, Behavioral, and Situational categories, with selectable difficulty and question count (5/10/15/20) in Practice mode, or a focused 10-question Mock Interview flow
- **Voice-enabled mock interviews** — answer out loud using the Web Speech API with live transcription, and have questions or feedback read back to you
- **Performance dashboard** — Recharts-powered analytics split into Overall / Mock / Practice views, tracking average score, sessions completed, and answers attempted
- **Secure authentication** — email/password (bcrypt-hashed) or one-tap Google Sign-In, backed by short-lived JWT access tokens and rotating refresh tokens in httpOnly cookies, with automatic silent session refresh on the client
- **Large question bank** — up to 10,000 auto-generated technical, behavioral, and situational questions each, parsed from Markdown datasets and automatically tagged with a difficulty level
- **Hardened Express API** — Helmet security headers, a strict CORS allow-list, per-route rate limiting, request validation middleware, and structured JSON logging with request IDs
- **Health & readiness checks** — `/health/live` and `/health/ready` endpoints for uptime and database-readiness monitoring
- **Contact form** — visitor messages are validated and stored in MongoDB
- **Dockerized** — ready-to-run Dockerfiles for the frontend (unprivileged Nginx) and backend (non-root user), wired together with a single `docker-compose.yml`
- **CI on every push** — GitHub Actions installs and tests both the backend and frontend on every push and pull request
- **Free-tier deployment guide** — a step-by-step walkthrough for MongoDB Atlas + Vercel + Netlify in [`DEPLOYMENT.md`](./DEPLOYMENT.md)

## Tech Stack

**Frontend**

- React 19 + Vite 7
- Tailwind CSS 3
- React Router 7
- Axios
- Recharts (dashboard charts)
- lucide-react (icons)
- Vitest + React Testing Library (tests)

**Backend**

- Node.js + Express 5
- MongoDB + Mongoose 8
- JWT (`jsonwebtoken`) + `bcryptjs`
- Helmet, CORS, `express-rate-limit`
- OpenAI SDK (optional AI evaluation)
- Python 3 (optional local evaluator script)
- Node's built-in test runner (`node --test`)

**DevOps**

- Docker + Docker Compose
- GitHub Actions (CI)
- Nginx (frontend container) / Vercel serverless (alternative backend host)

## Screenshots

| Landing Page                                                                                              | Login                                                                                               |
| --------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| ![Landing page](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/03-hero.png) | ![Login](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/02-login.png) |

| Sign Up                                                                                                  | Contact                                                                                                 |
| -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| ![Sign up](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/01-register.png) | ![Contact](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/06-contact.png) |

| Practice Categories                                                                                                          | Features                                                                                                   |
| ---------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| ![Practice categories](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/05-practice-session.png) | ![Features](https://raw.githubusercontent.com/sadiqqgavvi-gif/smarthire/main/screenshots/04-functions.png) |

## Project Structure

```
smarthire/
├── SmartHire/                 # React + Vite frontend
│   ├── public/                  # Static assets (favicon/logo)
│   ├── src/
│   │   ├── components/           # Hero, Features, Practice, Testimonials, Contact
│   │   ├── layout/                 # Navbar, Footer
│   │   ├── pages/                   # Auth, Dashboard, ConfigurePractice, PracticeSession, MockInterview
│   │   ├── routes/                   # ProtectedRoute
│   │   ├── utils/                      # authFetch (session-aware fetch), apiBaseUrl
│   │   └── tests/                       # Vitest + Testing Library specs
│   ├── Dockerfile / nginx.conf
│   └── vite.config.js
├── smarthire-server/           # Express + MongoDB backend
│   ├── routes/                   # /api/auth, /api/questions, /api/practice, /api/mock, /api/ai, /api/contact
│   ├── controllers/                # Route handlers
│   ├── models/                       # User, Question, PracticeSession, Message
│   ├── middleware/                     # auth, validation, rate limiting, observability
│   ├── services/                         # aiEvaluationService, smartEvaluator, pythonEvaluationService, guardrails
│   ├── python/                             # evaluate_answer.py (optional Python evaluator)
│   ├── scripts/                             # dataset build/generation scripts
│   ├── topics/                                # generated technical/behavioral/situational question banks (Markdown)
│   ├── tests/                                  # Node test runner specs
│   └── Dockerfile
├── screenshots/                 # Images used in this README
├── docker-compose.yml
├── DEPLOYMENT.md
├── netlify.toml
└── .github/workflows/ci.yml    # CI: installs + tests frontend & backend
```

## Getting Started

### Prerequisites

- Node.js 20+
- npm
- MongoDB (local install, Docker, or a free [MongoDB Atlas](https://www.mongodb.com/atlas) cluster)
- Python 3 — optional, only needed if `USE_PYTHON_EVALUATOR=true`

### 1. Clone the repo

```bash
git clone https://github.com/sadiqqgavvi-gif/smarthire.git
cd smarthire
```

### 2. Fastest path: Docker Compose

From the repo root:

```bash
docker compose up --build
```

- Frontend: `http://localhost:5173`
- Backend API: `http://localhost:5000`
- MongoDB: `mongodb://localhost:27017`

Stop everything with `docker compose down`.

### 3. Manual setup

**Backend**

```bash
cd smarthire-server
npm install
```

Create `smarthire-server/.env`:

```env
# Server
NODE_ENV=development
PORT=5000
CLIENT_ORIGINS=http://localhost:5173,http://localhost:5174

# Database
MONGO_URI=mongodb://localhost:27017/smarthire
MONGO_CONNECT_TIMEOUT_MS=10000

# Auth
JWT_SECRET=replace_with_a_long_random_string
ACCESS_TOKEN_SECRET=replace_with_a_long_random_string
REFRESH_TOKEN_SECRET=replace_with_a_long_random_string
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# AI evaluation (all optional — the app works without any of these)
USE_AI=false
OPENAI_API_KEY=your_key_here
OPENAI_EVALUATION_MODEL=gpt-4o-mini
USE_PYTHON_EVALUATOR=true
PYTHON_BIN=python3
PYTHON_EVALUATOR_TIMEOUT_MS=12000

# Rate limiting (defaults shown)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=250
AUTH_RATE_LIMIT_MAX=40
AI_RATE_LIMIT_MAX=20
MOCK_EVAL_RATE_LIMIT_MAX=25

# Logging
LOG_HTTP_REQUESTS=true
```

> On Windows, set `PYTHON_BIN=python` instead of `python3` if that's how your interpreter is aliased.

Run it:

```bash
npm start
```

**Frontend**

```bash
cd SmartHire
npm install
```

Create `SmartHire/.env`:

```env
VITE_API_URL=http://localhost:5000
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
```

Run it:

```bash
npm run dev
```

The app will be available at `http://localhost:5173`.

> Keep `.env` files private and out of version control — both are already covered by `.gitignore`.

### Rebuild the question dataset (optional)

To refresh the MongoDB `Question` collection from the Markdown sources under `smarthire-server/topics/`:

```bash
cd smarthire-server
npm run build:practice
```

`npm start` also runs this automatically via a `prestart` hook. To regenerate the underlying synthetic Markdown datasets themselves (technical, behavioral, and situational), use `npm run generate:questions`.

## How Answer Evaluation Works

Every submitted answer flows through a resilient, three-tier evaluator so scoring keeps working even without an AI budget:

1. **OpenAI** — used when `USE_AI=true` and a valid `OPENAI_API_KEY` is set. The optional Python evaluator can call OpenAI itself when `USE_PYTHON_EVALUATOR=true`; otherwise the Node service calls it directly.
2. **Python heuristic evaluator** — if AI is disabled, unset, or the request fails, `python/evaluate_answer.py` scores the answer locally using keyword and structure matching.
3. **Smart Node fallback** — if the Python evaluator isn't enabled at all, a built-in JavaScript heuristic (`services/smartEvaluator.js`) takes over.

Whichever tier produces a result, it passes through a shared **guardrails** layer (`services/evaluationGuardrails.js`) that caps scores for answers that are too short, off-topic, or nonsensical — so a lucky keyword match can't inflate a low-effort answer into a high score.

## API Overview

| Method | Endpoint                    | Description                                                  |
| ------ | --------------------------- | ------------------------------------------------------------ |
| POST   | `/api/auth/register`        | Create an account with email + password                      |
| POST   | `/api/auth/login`           | Log in with email + password                                 |
| POST   | `/api/auth/google`          | Log in / sign up with a Google ID token                      |
| GET    | `/api/auth/me`              | Get the current session's user _(protected)_                 |
| POST   | `/api/auth/refresh`         | Rotate the access/refresh tokens                             |
| POST   | `/api/auth/logout`          | Invalidate the current refresh token                         |
| GET    | `/api/questions`            | Fetch questions by `category`, `difficulty`, `role`, `count` |
| GET    | `/api/practice/:type`       | Fetch questions for a Practice/Mock session                  |
| POST   | `/api/practice/sessions`    | Save a completed session _(protected)_                       |
| GET    | `/api/practice/sessions/me` | Dashboard stats + session history _(protected)_              |
| GET    | `/api/mock/:category`       | Fetch a randomized mock interview question set               |
| POST   | `/api/mock/evaluate`        | Evaluate a mock interview answer                             |
| POST   | `/api/ai/evaluate`          | Evaluate a practice session answer                           |
| GET    | `/api/ai/python-health`     | Diagnostics for the optional Python evaluator                |
| POST   | `/api/contact`              | Submit the contact form                                      |
| GET    | `/health/live`              | Liveness probe                                               |
| GET    | `/health/ready`             | Readiness probe (checks the MongoDB connection)              |

## Testing

Backend:

```bash
cd smarthire-server
npm test
```

Frontend:

```bash
cd SmartHire
npm test
```

GitHub Actions (`.github/workflows/ci.yml`) runs both suites on every push and pull request.

## Deployment

Two supported paths:

- **Docker Compose** — bundles MongoDB, the backend, and an Nginx-served frontend build; good for a VPS or local demo.
- **Free-tier hosted** — MongoDB Atlas + Vercel (backend, served serverlessly via `smarthire-server/api/index.js`) + Netlify or Vercel (frontend). Full step-by-step instructions, including which environment variables to set on each platform, are in [`DEPLOYMENT.md`](./DEPLOYMENT.md).

## Roadmap

- Resume-based, personalized question generation
- Downloadable PDF reports per interview session
- Admin panel for managing the question bank
- Role-specific interview tracks (frontend, backend, QA, data)
- Deeper, more calibrated AI evaluation rubrics
- Optional video/posture analysis (pending ethical review)

## About This Project

SmartHire began as a Final Year Project (BS Software Engineering) by **Gavriel Sadiq** and **Faizan Habib**, supervised by Ms. Iram Rubab, Department of Software Engineering, PMAS-Arid Agriculture University Rawalpindi. Additional FYP documentation — UML diagrams and an interview guide — is included in the repo root.

## Live Demo

<!-- Once deployed, replace this line with your live URL, e.g.: -->
<!-- 🔗 [Try it live](https://your-deployed-app.vercel.app) -->

_Live demo coming soon._

## License

This project is open source and available for review as part of my portfolio.
